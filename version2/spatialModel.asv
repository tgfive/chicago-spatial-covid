clear
clc
close all;

domain = shaperead('boundaries.shp','UseGeoCoords',true);
h = height(domain);
faceColors = makesymbolspec('Polygon',...
    {'INDEX',[1 h],'FaceColor',polcmap(h)});

geoshow(domain,'SymbolSpec',faceColors)

poly = simplify(polyshape(domain.Lon,domain.Lat));
plot(pgon)